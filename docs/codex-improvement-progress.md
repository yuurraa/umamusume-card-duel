# Codex improvement progress

This record reflects local implementation work begun on 2026-09-05. Browser, two-device WebRTC, cloud persistence, accessibility, and performance checks have not been represented as completed without direct evidence.

| Task ID | Status | Changed files | Checks run | Result / remaining limitation |
| --- | --- | --- | --- | --- |
| BASE-01 | done | `package.json`, `frontend/package.json`, `frontend/vite.config.ts`, `frontend/src/match/feedback/overlays.test.tsx`, `backend/package.json`, `backend/src/tests/pvpProtocolScenarios.ts` | `npm run build`, `npm run test` (Linux Node 22.12.0) | Root build and test entry points are available. Existing AI scenarios, focused protocol scenarios, and React/jsdom fake-timer overlay scenarios pass. |
| ANIM-01 | done | `frontend/src/match/feedback/BattleEffectOverlay.tsx`, `frontend/src/match/feedback/overlays.test.tsx` | React/jsdom fake-timer tests, build | Battle completion now depends on event identity/duration, uses the current callback via a ref, and is idempotent. The suite covers fresh callback rerenders, cancellation on event replacement/unmount, and StrictMode completion. |
| ANIM-02 | done | `frontend/src/match/feedback/CardFlowOverlay.tsx`, `frontend/src/app/MatchOverlays.tsx`, `frontend/src/match/feedback/overlays.test.tsx` | React/jsdom fake-timer tests, build | Empty/non-empty transitions now obey hook ordering and have a direct regression test. Card flow waits for every image to settle before starting, and uses one idempotent batch recovery barrier rather than the final indexed card. Battle batches now wait for every member, rather than an implicitly last item. Browser animation/image-failure verification remains unverified. |
| ANIM-03 | in_progress | `frontend/src/app/hooks/{useBattleVisuals,useCardFlowVisuals,useQueuedVisualActions}.ts`, `frontend/src/app/hooks/useAppNavigation.ts`, `frontend/src/app/AppRoot.tsx`, `frontend/src/app/MatchOverlays.tsx`, `frontend/src/match/feedback/CardFlowOverlay.tsx` | Frontend tests (32 tests), build, lint | KO dissolve, vacancy, promotion, and card-flow completion callbacks carry visual-generation guards and are ignored after reset. Deferred visual actions now have a focused queue hook with explicit flush/clear semantics; new local matches invoke the same centralized transient reset as rematches, clearing queued actions, coin/card-flow state, retained KO state, deferred-hand and game-over timers before installing replacement state. Browser interruption coverage remains pending. |
| PERF-01 | in_progress | `frontend/src/match/feedback/BattleEffectOverlay.tsx`, `frontend/src/app/animation/battleEffectKeyframes.ts`, `frontend/src/app/MatchOverlays.tsx`, `frontend/src/components/{cards/UmaCard.tsx,boards/Bench.tsx}`, `frontend/src/app/animation/battleEffects.ts`, `frontend/src/app/hooks/useBattleVisuals.ts`, `frontend/src/match/feedback/{overlays.test.tsx,battleEffects.test.ts}` | React/jsdom regression tests (32 frontend tests), 51 AI scenarios, build, lint; manual live-browser AI-vs-AI KO | Battle keyframes are mounted by the stable match-overlay owner from a shared style module instead of injected per effect. Each logical transition now captures fresh per-card geometry, preventing a persistent card UID from pinning effects to the position where it started; the overlay reuses that event geometry without another lookup. A regression verifies changed DOM geometry is captured on the next snapshot. KO animations preserve the card's current hover/focus transform instead of snapping upright on their first frame, and use transform/opacity rather than animating image filters. A coin-triggered KO regression now verifies pre-KO HP in structured attack events. A manual AI-vs-AI match was played through a real KO: the KO banner and point update appeared cleanly, with no visible hitch or stalled transition. Broader browser profiler measurements and scenarios remain pending. |
| ANIM-04 | in_progress | `frontend/src/app/hooks/useReducedMotion.ts`, `frontend/src/app/AppRoot.tsx`, `frontend/src/app/MatchOverlays.tsx`, `frontend/src/match/feedback/PointGainOverlay.tsx`, `frontend/src/global.css` | Type/build check | Motion preference is centralized, applies at the next sequence boundary, shortens battle/card-flow/point timing, and suppresses decorative motion through a durable global rule. Full setup-to-game-over browser verification remains pending. |
| NET-01 | done | `frontend/src/pvp/protocol.ts`, `frontend/src/app/hooks/usePvpMatch.ts`, `shared/src/localDecks.ts`, `backend/src/tests/pvpProtocolScenarios.ts` | Focused protocol scenarios, build | Runtime validation covers hello, helloAck, intents, and the complete sync shape. It validates known IDs in visible zones/instances while accepting only deliberate redacted placeholders in private zones. The host applies shared card, duplicate, basic-card, and energy rules before acknowledging a guest deck. Corrupt gzip and highly compressible oversized decompressed payloads are rejected within the bounded inflater. |
| NET-02 | done | `frontend/src/pvp/peer.ts`, `frontend/src/pvp/orderedReceiver.ts`, `frontend/src/pvp/outbox.ts`, `frontend/src/pvp/outbox.test.ts`, `backend/src/tests/pvpProtocolScenarios.ts` | Ordered receiver/outbox fixtures, `npm run test`, build, lint | Receive decoding is serialized and connection-generation scoped; failed decoding cannot block later messages. A bounded outbound outbox now preserves FIFO sends through asynchronous encoding, coalesces superseded sync snapshots, and clears stale work on connection replacement. Transport-neutral fixtures verify delayed/malformed receive packets and bounded/coalesced send work. Full browser WebRTC reconnect testing remains an external check. |
| NET-03 | in_progress | `frontend/src/pvp/{protocol,stateMirror}.ts`, `frontend/src/app/hooks/usePvpMatch.ts`, `frontend/src/app/matchPerspective.ts`, `backend/src/tests/pvpProtocolScenarios.ts`, `docs/pvp-perspective-boundary.md` | Perspective/privacy/session/version/cursor fixtures, build | Host sync packets carry protocol version, a per-session identity, monotonic sequence, retained-history start, and event cursor; a newly identified guest session resets the host stream cursor so the retained structured-event window is replayed on reconnect. Guests clear stale retained presentation events when the history-start marker proves a gap, using the canonical snapshot as the deterministic resync source. Incompatible versions are rejected, guests reject packets from another session or with duplicate/stale state, and hosts reject obsolete-session intents. Host private hand/deck/opening-hand IDs are redacted on the wire, and private-zone log entries are redacted before transport. The suite checks wire and guest-projection secrecy, mirror-twice canonical equivalence, and explicit swapping of `humanBySide`/`aiDeckStyleBySide` in both PvP and display projections. Real WebRTC reconnect testing remains unverified. |
| RULE-02 | in_progress | `frontend/src/game/engine.ts`, `frontend/src/game/engine/flow/eligibility.ts`, `frontend/src/game/engine/flow/combat.ts`, `frontend/src/app/gameUiHelpers.ts`, `frontend/src/pvp/playerIntent.ts`, `backend/src/tests/aiCombatScenarios.ts` | AI/rule scenario suite (51 scenarios), build, lint | Selected attacks validate their own energy costs and reject out-of-range indexes without state mutation. Shared `canUseAnyAttack` eligibility and coin-preview checks now honor the selected attack index instead of silently falling back to the primary attack. Target-any attacks and abilities reject missing/invalid target IDs instead of silently substituting the opponent Active; supplied heal/switch/evolution/discard targets are validated against the selected attack before mutation. Trainers requiring selected targets reject invalid IDs before card consumption, invalid retreat targets reject before Energy payment, and repeated opening-coin choices no longer allocate a transition. Structured attack events preserve the target's pre-KO HP even when a coin effect sets HP to zero before normal damage resolution. Terminal matches now reject player/AI actions without mutation, and trainer-play coverage checks card conservation across hand, deck, discard, and in-play zones. Retreat payments and Energy discard effects now emit typed negative Energy events. `applyPlayerIntentWithResult` now exposes a typed accepted/rejected result and regressions prove rejected intents preserve canonical state. Broader invariant coverage remains pending. |
| RULE-01 | done | `shared/src/types.ts`, `frontend/src/game/engine.ts`, `frontend/src/game/engine/core/random.ts`, `frontend/src/game/engine/flow/setup.ts`, `frontend/src/game/engine/flow/turn.ts`, `frontend/src/game/engine/flow/combat.ts`, `frontend/src/game/engine/flow/trainers.ts`, `frontend/src/game/engine/flow/playRules.ts`, `frontend/src/game/engine/flow/ai/{core,types}.ts`, `backend/src/tests/aiCombatScenarios.ts` | `rg` random-call audit, `npm run test`, `npm run build` (Linux Node 22.12.0), `npm run lint` (Linux Node 24.14.0) | Instance IDs are serializable match state, not a module-global counter; interleaved-match coverage verifies isolated sequences. A narrow `RandomSource` now flows through setup, coins, attacks, turn energy, trainers/stadiums, abilities, AI combat/trainer paths, and promotion/timeout transitions, defaulting to real randomness for normal play. Seeded regressions prove reproducible setup, coin, and energy outcomes; full fast checks pass. |
| RULE-03 | in_progress | `frontend/src/pvp/playerIntent.ts`, `frontend/src/pvp/{peer,outbox,outbox.test}.ts`, `frontend/src/app/hooks/usePvpMatch.ts`, `frontend/src/app/hooks/useAppRuntimeEffects.ts`, `frontend/src/app/hooks/useAppNavigation.ts`, `frontend/src/app/AppRoot.tsx`, `frontend/src/app/hooks/useCardFlowVisuals.ts`, `frontend/src/game/engine.ts`, `frontend/src/game/engine/core/random.ts`, `frontend/src/game/engine/flow/ai/telemetry.test.ts`, `backend/src/tests/aiCombatScenarios.ts` | Source audit for outbound sync in state updaters, replayable-random/telemetry/outbox regressions, `npm run test`, build, lint | Host-local intents, guest intents, deadline assignment, setup countdown/coin resolution, and opening-hand completion now resolve outside React updater callbacks. AI setup coin and AI turn state updaters use a replayable random source so repeated React updater evaluation cannot reroll a committed outcome. `createGame` no longer clears shared telemetry; explicit app/PvP match-start boundaries clear the opt-in development panel instead, with a regression proving independent engine construction preserves existing records. Invalid intents do not rerender or send unchanged sync packets. The command result carries accepted/rejected status, the transition ID, and newly emitted structured events. A bounded in-memory outbound outbox now protects ordered sends through async encoding and channel readiness; persistence across page reload/process restart remains outside this client transport scope. |
| UX-01 | in_progress | `frontend/src/match/useModalFocus.ts`, `frontend/src/match/feedback/overlays.test.tsx`, `frontend/src/components/buttons/NeutralButton.tsx`, `frontend/src/match/modals/{CardPreview,DeckChoiceModal,DiscardPileModal,EndTurnWarningModal,GameOverModal,OpponentZonesModal}.tsx`, `frontend/src/match/feedback/CoinFlipOverlay.tsx` | `npm run test` (32 frontend tests), build, lint | Core match modals use named `role="dialog"` and `aria-modal="true"` semantics. Shared focus handling now enters the first enabled control, contains Tab/Shift+Tab, handles Escape when a close action exists, and restores the invoking element; jsdom covers those behaviors. Keyboard-only match completion, touch behavior, and small-viewport checks still require direct browser evidence. |
| TYPE-01 | done | `frontend/src/app/MatchOverlays.tsx`, `frontend/src/screens/deck-browser/DeckBrowserDeckGrid.tsx`, `frontend/src/screens/deck-browser/DeckBrowserFilters.tsx`, `frontend/src/screens/deck-browser/DeckOpenedModal.tsx`, `frontend/src/screens/CardBrowserScreen.tsx`, `frontend/src/screens/DeckBrowserScreen.tsx`, `frontend/src/screens/deck-browser/DeckCardSelectorModal.tsx`, `frontend/eslint.config.mjs`, `frontend/package.json`, `package.json` | `npm run lint` (Linux Node 24.14.0), `npm run test`, `npm run build` (Linux Node 22.12.0) | Replaced the named blanket `any` component boundaries with explicit UI/domain contracts, including modal state setters and overlay callbacks. Hook dependency warnings were resolved rather than suppressed. Lint is clean and strict type/build checks pass. |
| DOC-01 | done | `README.md`, `docs/engine-dependency-map.md` | Current source-tree/import audit, documentation review | Replaced the placeholder README with workspace ownership, install/dev/build/test/lint commands, host-authority and privacy boundaries, deterministic testing guidance, and a concise card-effect workflow. Replaced the stale dependency report with a current ownership/boundary map for the public facade, rule flows, presentation, PvP projection, and tests. |
| EVENT-01 | in_progress | `shared/src/types.ts`, `frontend/src/game/engine/core/events.ts`, `frontend/src/game/engine/flow/{ai/abilityUtils,ai/core,board,combat,energy,evolution,trainers,turn}.ts`, `frontend/src/game/engine.ts`, `frontend/src/app/animation/{battleEffects,cardFlow}.ts`, `frontend/src/app/animation/{battleEffects,cardFlow}.test.ts`, `frontend/src/app/{gameUiHelpers,gameUiHelpers.test}.ts`, `frontend/src/app/hooks/useLogNotifications.ts`, `frontend/src/pvp/stateMirror.ts`, `frontend/src/pvp/protocol.ts`, `backend/src/tests/{aiCombatScenarios,pvpProtocolScenarios}.ts` | 51 AI scenarios, focused battle-effects/card-flow/structured-coin tests (32 frontend tests, including wording-independent lethal/played/hidden-draw mapping), PvP protocol test, frontend build/test/lint | Authoritative transitions now emit bounded, ordered, serializable events for attack, ability damage/heal/energy, KO, score, game end, promotion, coin, turn, card movement, energy, evolution, and status outcomes. Combat coin results for bonus/draw/discard attacks, multi-coin knockout attacks, and AI coin-flip abilities now join the same transition ledger, and the notification queue consumes those coin events rather than discovering them from English log wording. Player and AI ability resolutions share a transition scope; energy movement abilities now emit both source `-1` and active `+1` events. Card play, trainer draw/search/discard/shuffle/swap, stadium replacement, AI basic/evolution play, attack/ability hand discards, retreat payments and Energy discard costs, deck evolution, active-card shuffles, knockout-to-discard movements, board-normalization overflow discards, end-turn tool healing, poison damage, and bench-to-active energy movement now emit structured events. Opponent card identities are projected once a movement reaches public play/discard zones, and battle/card-flow adapters no longer classify visual effects from English log text; state diffs are used only to fill intentionally redacted movement identities/counts. Broader trainer/card-effect event coverage remains pending. |
| EVENT-02 | in_progress | `frontend/src/pvp/stateMirror.ts`, `frontend/src/pvp/protocol.ts`, `frontend/src/app/hooks/usePvpMatch.ts`, `frontend/src/game/engine/flow/turn.ts`, `backend/src/tests/pvpProtocolScenarios.ts` | PvP projection/cursor/history-gap fixture, protocol test, build, lint | Host sync now sends only recipient-visible structured event deltas with a monotonic event cursor and retained-history start marker; guests merge mirrored events by stable ID, clear stale retained presentation events when the marker proves an exhausted-history gap, and retain canonical state as the resync source. Signed Energy deltas are accepted on the wire for retreat/discard/source-transfer events. Private events and hidden-zone card identities are omitted, while played/discarded opponent card identities remain public. Browser reconnect remains unverified. |
| FLOW-01 | in_progress | `frontend/src/app/sequence/visualSequence.ts`, `frontend/src/app/sequence/visualSequence.test.ts`, `frontend/src/app/hooks/useBattleVisuals.ts`, `frontend/src/app/{AppRoot,MatchOverlays}.tsx` | Frontend tests (32 tests), build, lint | A pure precedence reducer now owns the visual phase/blocking decision across coin, battle, KO dissolve, point, promotion, card-flow, and pending-choice states. Battle, card-flow, point-gain, and selection-prompt visibility now derive from that single phase owner instead of repeating independent priority predicates; tests cover priority, pending-choice handoff, and KO-batch precedence. Existing KO/card-flow state remains in the hook while migration continues. |
| ARCH-03 | in_progress | `backend/src/{config,server}.ts`, `backend/src/start.ts`, `backend/src/routes/{localDeckRoutes,cloudDeckRoutes}.ts`, `backend/src/pvpRoutes.ts`, `backend/src/storage/{localDeckStore,cloudDeckStore}.ts`, `backend/src/tests/{configScenarios,serverConstruction,localDeckStoreScenarios,localDeckRoutesScenarios,cloudDeckStoreScenarios,pvpRoutesScenarios}.ts`, `backend/package.json`, `package.json`, `README.md` | backend build, config/default tests, ephemeral HTTP app tests, isolated local/cloud-fallback/signaling fixtures, injected cloud outage/auth rejection responses | `createApp` now owns injectable app construction while `startServer` remains the only startup binding; fixture stores and Firebase health dependencies can be supplied without production credentials. Local and cloud deck persistence now live behind focused, injectable route/store boundaries with malformed-file isolation, input validation, atomic writes, stable JSON 503 responses, preserved cloud API paths, and production-safe no-Firebase behavior. Unauthenticated cloud fallback and backend unlocks now require explicit development opt-in and are forced off in production; local-only API behavior is documented and tested. Cloud route authentication is injectable for boundary tests, and rejected auth is proven not to reach persistence. PvP signaling now has injectable clock/TTL coverage for answer/offer compatibility, candidate filtering, and expiration. Firebase-backed failure injection and broader authentication/persistence-failure coverage remain pending. |
| ARCH-02 | in_progress | `frontend/package.json`, `shared/package.json`, `frontend/src/game/engine.ts`, `frontend/src/game/engine/index.ts`, `frontend/src/app/{AiTelemetryPanel.tsx,animation/{battleEffects,cardFlow}.ts,hooks/useAppRuntimeEffects.ts}`, `frontend/src/match/modals/{CardPreview,DeckChoiceModal}.tsx`, `frontend/src/screens/{CardBrowserScreen.tsx,card-browser/model.ts,deck-browser/{DeckBrowserFilters,DeckCardSelectorModal,helpers,model}.ts}`, `frontend/src/utils/cardSorting.ts`, `frontend/src/pvp/playerIntent.ts`, `backend/src/tests/{aiCombatScenarios,packageResolution}.ts`, `frontend/src/app/animation/battleEffects.test.ts`, `docs/engine-dependency-map.md` | Deep-import audit, package-resolution scenario, frontend tests, build, lint | The UI-independent engine facade now re-exports the supported match fixture/board helpers used by backend and presentation regressions, plus presentation-safe labels, constants, event helpers, replayable randomness, and debug telemetry types. Narrow workspace exports now expose shared data and the public engine facade; a runtime scenario resolves both package entry points without deep engine imports. App, screens, match presentation, PvP intents, and utility code now cross the documented facade; the directory compatibility entrypoint now re-exports the same facade instead of maintaining a second stale export list, and no remaining frontend/backend imports target `game/engine/core` or `game/engine/flow` outside engine-internal modules. Migrating every shared relative import remains optional follow-up work. |
| PERF-02 | in_progress | `frontend/src/app/LazyLoadErrorBoundary.tsx`, `frontend/src/app/LazyLoadErrorBoundary.test.tsx`, `frontend/src/app/nonMatchScreens.tsx`, `frontend/src/app/AppRoot.tsx` | 32 frontend tests, build, lint | Lazy screen and match-board chunk failures now render a recoverable reload action instead of an unexplained blank/indefinite Suspense state. Initial/first-match transfer measurements and asset-cache profiling remain pending. |
| ARCH-01 | in_progress | `frontend/src/app/AppRoot.tsx`, `frontend/src/app/hooks/{useMatchCommandController,useQueuedVisualActions}.ts`, `frontend/src/app/hooks/useQueuedVisualActions.test.ts` | 32 frontend tests, build, lint | Deferred visual actions now belong to a focused hook with explicit queue/flush/clear ownership, reducing raw coordination state in `AppRoot`. Setup countdown, opening-coin resolution, and attack-shuffle commands now share a typed command-controller boundary that keeps canonical transition and host-sync behavior together. Broader match-controller extraction, modal ownership consolidation, and browser lifecycle verification remain pending. |
| UX-02 / VERIFY-01 | deferred | — | — | Browser pacing/accessibility acceptance and the final cross-mode/device matrix require direct browser/device evidence. |

## Local validation notes

- The default `npm` on this machine resolves to Windows and fails from this WSL path. Validation used Linux Node 22.12.0 by putting its existing NVM bin directory first in `PATH`; no project configuration was changed for that environment issue.
- The configured lint version requires Linux Node 24.14.0 in this workspace; it completed with no warnings. Build and tests were run with Linux Node 22.12.0.
- This WSL task's desktop automation helper rejects the workspace path before it can enumerate a browser window, so it cannot itself perform direct browser/device checks. Separately, a manual live-browser AI-vs-AI KO check completed cleanly with no visible hitch or stalled transition; the remaining browser-dependent rows stay unverified unless their specific scenario is recorded.
- The linked Windows-native checkout at `C:\\Users\\joshu\\Documents\\umamusume-card-duel` builds its frontend successfully with Windows Node. Its installed dependencies are platform-native, so Linux Node cannot reuse them; direct browser automation still requires the task's desktop-control session to be opened from that Windows-native workspace rather than this WSL-bound task.
- No commits, pushes, pull requests, deployments, or remote-service mutations were performed.
