# Codex improvement progress

This record reflects local implementation work begun on 2026-09-05. Browser, two-device WebRTC, cloud persistence, accessibility, and performance checks have not been represented as completed without direct evidence.

| Task ID | Status | Changed files | Checks run | Result / remaining limitation |
| --- | --- | --- | --- | --- |
| BASE-01 | done | `package.json`, `frontend/package.json`, `frontend/vite.config.ts`, `frontend/src/match/feedback/overlays.test.tsx`, `backend/package.json`, `backend/src/tests/pvpProtocolScenarios.ts` | `npm run build`, `npm run test` (Linux Node 22.12.0) | Root build and test entry points are available. Existing AI scenarios, focused protocol scenarios, and React/jsdom fake-timer overlay scenarios pass. |
| ANIM-01 | done | `frontend/src/match/feedback/BattleEffectOverlay.tsx`, `frontend/src/match/feedback/overlays.test.tsx` | React/jsdom fake-timer tests, build | Battle completion now depends on event identity/duration, uses the current callback via a ref, and is idempotent. The suite covers fresh callback rerenders, cancellation on event replacement/unmount, and StrictMode completion. |
| ANIM-02 | done | `frontend/src/match/feedback/CardFlowOverlay.tsx`, `frontend/src/app/MatchOverlays.tsx`, `frontend/src/match/feedback/overlays.test.tsx` | React/jsdom fake-timer tests, build | Empty/non-empty transitions now obey hook ordering and have a direct regression test. Card flow waits for every image to settle before starting, and uses one idempotent batch recovery barrier rather than the final indexed card. Battle batches now wait for every member, rather than an implicitly last item. Browser animation/image-failure verification remains unverified. |
| ANIM-03 | in_progress | `frontend/src/app/hooks/{useBattleVisuals,useCardFlowVisuals,useQueuedVisualActions}.ts`, `frontend/src/app/hooks/useAppNavigation.ts`, `frontend/src/app/AppRoot.tsx`, `frontend/src/app/MatchOverlays.tsx`, `frontend/src/match/feedback/CardFlowOverlay.tsx`, `frontend/src/app/animation/battleEffects.ts` | Frontend tests (57 tests), build, lint | KO dissolve, vacancy, promotion, and card-flow completion callbacks carry visual-generation guards and are ignored after reset. Deferred visual actions now have a focused queue hook with explicit flush/clear semantics; new local matches invoke the same centralized transient reset as rematches, clearing queued actions, coin/card-flow state, retained KO state, deferred-hand and game-over timers before installing replacement state. A newly detected knockout now retains the previous board during the render/layout-effect handoff, preventing a one-frame defeated-card disappearance before the KO overlay is queued. Browser interruption coverage remains pending. |
| PERF-01 | in_progress | `frontend/src/match/feedback/BattleEffectOverlay.tsx`, `frontend/src/app/animation/battleEffectKeyframes.ts`, `frontend/src/app/MatchOverlays.tsx`, `frontend/src/components/{cards/UmaCard.tsx,boards/Bench.tsx}`, `frontend/src/app/animation/{battleEffects,battleEffects.test}.ts`, `frontend/src/app/hooks/useBattleVisuals.ts`, `frontend/src/match/feedback/{overlays.test.tsx,battleEffects.test.ts}` | React/jsdom regression tests (57 frontend tests), 63 AI scenarios, build, lint; manual live-browser AI-vs-AI KO | Battle keyframes are mounted by the stable match-overlay owner from a shared style module instead of injected per effect. Each logical transition now captures fresh per-card geometry, preventing a persistent card UID from pinning effects to the position where it started; the overlay reuses that event geometry without another lookup. Regressions verify changed DOM geometry is captured on the next snapshot and that a newly removed KO target is retained from the previous snapshot before the visual queue commits. KO animations preserve the card's current hover/focus transform instead of snapping upright on their first frame, and use transform/opacity rather than animating image filters. A coin-triggered KO regression now verifies pre-KO HP in structured attack events. A manual AI-vs-AI match was played through a real KO: the KO banner and point update appeared cleanly, with no visible hitch or stalled transition. Broader browser profiler measurements and scenarios remain pending. |
| ANIM-04 | in_progress | `frontend/src/app/hooks/{useReducedMotion,useReducedMotion.test}.tsx`, `frontend/src/app/AppRoot.tsx`, `frontend/src/app/MatchOverlays.tsx`, `frontend/src/match/feedback/PointGainOverlay.tsx`, `frontend/src/global.css` | Focused motion-preference hook test (2 tests), frontend type/lint check | Motion preference is centralized, applies at the next sequence boundary, shortens battle/card-flow/point timing, and suppresses decorative motion through a durable global rule. The hook regression verifies initial system preference, live preference changes, and media-query-listener cleanup. Full setup-to-game-over browser verification remains pending. |
| NET-01 | done | `frontend/src/pvp/protocol.ts`, `frontend/src/app/hooks/usePvpMatch.ts`, `shared/src/localDecks.ts`, `backend/src/tests/pvpProtocolScenarios.ts` | Focused protocol scenarios, build | Runtime validation covers hello, helloAck, intents, and the complete sync shape. It validates known IDs in visible zones/instances while accepting only deliberate redacted placeholders in private zones. The host applies shared card, duplicate, basic-card, and energy rules before acknowledging a guest deck. Corrupt gzip and highly compressible oversized decompressed payloads are rejected within the bounded inflater. |
| NET-02 | done | `frontend/src/pvp/peer.ts`, `frontend/src/pvp/orderedReceiver.ts`, `frontend/src/pvp/outbox.ts`, `frontend/src/pvp/outbox.test.ts`, `backend/src/tests/pvpProtocolScenarios.ts` | Ordered receiver/outbox fixtures, `npm run test`, build, lint | Receive decoding is serialized and connection-generation scoped; failed decoding cannot block later messages. A bounded outbound outbox now preserves FIFO sends through asynchronous encoding, coalesces superseded sync snapshots, and clears stale work on connection replacement. Transport-neutral fixtures verify delayed/malformed receive packets and bounded/coalesced send work. Full browser WebRTC reconnect testing remains an external check. |
| NET-03 | in_progress | `frontend/src/pvp/{peer,protocol,stateMirror}.ts`, `frontend/src/pvp/peer.test.ts`, `frontend/src/app/hooks/usePvpMatch.ts`, `frontend/src/app/matchPerspective.ts`, `backend/src/tests/pvpProtocolScenarios.ts`, `docs/pvp-perspective-boundary.md` | Perspective/privacy/session/version/cursor fixtures; focused initial-ICE offer/answer test (2 tests); malformed duplicate-event wire fixture; frontend type/lint check | Host sync packets carry protocol version, a per-session identity, monotonic sequence, retained-history start, and event cursor; a newly identified guest session resets the host stream cursor so the retained structured-event window is replayed on reconnect. Guests clear stale retained presentation events when the history-start marker proves a gap, using the canonical snapshot as the deterministic resync source. Incompatible versions are rejected, including the version-2 structured-status-event schema, guests reject packets from another session or with duplicate/stale state, and hosts reject obsolete-session intents. Sync event arrays now also require strictly increasing event IDs, preventing duplicate/out-of-order retained presentation entries from crossing the wire. Host private hand/deck/opening-hand IDs are redacted on the wire, and private-zone log entries are redacted before transport. The initial offer and answer now wait for candidate gathering and embed that set in SDP, while retaining later candidate signaling, so a joining peer cannot depend solely on an absent trickle update; fake-peer regressions verify candidate embedding for both paths. The suite checks wire and guest-projection secrecy, mirror-twice canonical equivalence, and explicit swapping of `humanBySide`/`aiDeckStyleBySide` in both PvP and display projections. Real WebRTC reconnect testing remains pending against the deployed build. |
| RULE-02 | in_progress | `frontend/src/game/engine.ts`, `frontend/src/game/engine/flow/{eligibility,combat,retreat}.ts`, `frontend/src/app/gameUiHelpers.ts`, `frontend/src/pvp/playerIntent.ts`, `backend/src/tests/aiCombatScenarios.ts` | AI/rule scenario suite (63 scenarios), build, lint | Selected attacks validate their own energy costs and reject out-of-range indexes without state mutation. Shared `canUseAnyAttack` eligibility and coin-preview checks now honor the selected attack index instead of silently falling back to the primary attack. Target-any attacks and abilities reject missing/invalid target IDs instead of silently substituting the opponent Active; supplied heal/switch/evolution/discard targets are validated against the selected attack before mutation. Trainers requiring selected targets reject invalid IDs before card consumption, invalid retreat targets reject before Energy payment, invalid hand/deck/tool/discard selections no longer fall back to an unrelated card, malformed trainer-choice objects are rejected without throwing, and repeated opening-coin choices no longer allocate a transition. Setup selection now rejects malformed/non-integer, duplicate, overlapping, or non-Basic active/bench indexes without coercion, exceptions, or mutation. Explicit Energy targets no longer fall back to Active when the UID is invalid, pending-choice intents are restricted to the owning side, hand/attack indexes reject runtime coercion, optional evolution/random-discard attack indexes, and ability discard indexes are strict. Retreat selections now reject unknown Energy types instead of allowing an unpaid retreat. Pending replacement choices now validate a living bench target before cleanup, preserving canonical state for stale/dead/duplicate bench data. Structured attack events preserve the target's pre-KO HP even when a coin effect sets HP to zero before normal damage resolution. Terminal matches now reject player/AI actions without mutation, and trainer-play coverage checks card conservation across hand, deck, discard, and in-play zones. Retreat payments and Energy discard effects now emit typed negative Energy events. `applyPlayerIntentWithResult` now exposes a typed accepted/rejected result and regressions prove rejected intents preserve canonical state. A chained-transition invariant regression now checks unique instance IDs, HP/Energy bounds, single-condition state, monotonic retained event IDs, next-event allocation, and terminal-state coherence. Broader invariant coverage remains pending. |
| RULE-01 | done | `shared/src/types.ts`, `frontend/src/game/engine.ts`, `frontend/src/game/engine/core/random.ts`, `frontend/src/game/engine/flow/setup.ts`, `frontend/src/game/engine/flow/turn.ts`, `frontend/src/game/engine/flow/combat.ts`, `frontend/src/game/engine/flow/trainers.ts`, `frontend/src/game/engine/flow/playRules.ts`, `frontend/src/game/engine/flow/ai/{core,types}.ts`, `backend/src/tests/aiCombatScenarios.ts` | `rg` random-call audit, `npm run test`, `npm run build` (Linux Node 22.12.0), `npm run lint` (Linux Node 24.14.0) | Instance IDs are serializable match state, not a module-global counter; interleaved-match coverage verifies isolated sequences. A narrow `RandomSource` now flows through setup, coins, attacks, turn energy, trainers/stadiums, abilities, AI combat/trainer paths, and promotion/timeout transitions, defaulting to real randomness for normal play. Seeded regressions prove reproducible setup, coin, and energy outcomes; full fast checks pass. |
| RULE-03 | in_progress | `frontend/src/pvp/playerIntent.ts`, `frontend/src/pvp/{peer,outbox,outbox.test}.ts`, `frontend/src/app/hooks/usePvpMatch.ts`, `frontend/src/app/hooks/useAppRuntimeEffects.ts`, `frontend/src/app/hooks/useAppNavigation.ts`, `frontend/src/app/AppRoot.tsx`, `frontend/src/app/hooks/useCardFlowVisuals.ts`, `frontend/src/game/engine.ts`, `frontend/src/game/engine/core/random.ts`, `frontend/src/game/engine/flow/ai/telemetry.test.ts`, `backend/src/tests/aiCombatScenarios.ts` | Source audit for outbound sync in state updaters, replayable-random/telemetry/outbox regressions, `npm run test`, build, lint | Host-local intents, guest intents, deadline assignment, setup countdown/coin resolution, and opening-hand completion now resolve outside React updater callbacks. AI setup coin and AI turn state updaters use a replayable random source so repeated React updater evaluation cannot reroll a committed outcome. `createGame` no longer clears shared telemetry; explicit app/PvP match-start boundaries clear the opt-in development panel instead, with a regression proving independent engine construction preserves existing records. Invalid intents do not rerender or send unchanged sync packets. The command result carries accepted/rejected status, the transition ID, and newly emitted structured events. A bounded in-memory outbound outbox now protects ordered sends through async encoding and channel readiness; persistence across page reload/process restart remains outside this client transport scope. |
| UX-01 | in_progress | `frontend/src/match/useModalFocus.ts`, `frontend/src/match/feedback/{CoinFlipOverlay,CoinFlipOverlay.test,overlays.test}.tsx`, `frontend/src/components/buttons/NeutralButton.tsx`, `frontend/src/match/modals/{CardPreview,DeckChoiceModal,DiscardPileModal,EndTurnWarningModal,GameOverModal,OpponentZonesModal}.tsx`, `frontend/src/app/{AppRoot,MatchOverlays}.tsx`, `frontend/src/app/hooks/useMatchCommandController.ts`, `frontend/src/app/hooks/useMatchCommandController.test.tsx` | `npm run test` (57 frontend tests), type/lint check | Core match modals use named `role="dialog"` and `aria-modal="true"` semantics. Shared focus handling now enters the first enabled control, contains Tab/Shift+Tab, handles Escape when a close action exists, and restores the invoking element; jsdom covers those behaviors. The opening-coin prompt now renders heads/tails controls only for the host; joining peers see a waiting state with no inert controls, and the command boundary rejects a joiner call even if UI state is bypassed. Terminal game state unmounts that prompt and clears its pending state, preventing an opening-coin overlay from covering the game-over screen after a pre-game surrender or disconnect. Keyboard-only match completion, touch behavior, and small-viewport checks still require direct browser evidence. |
| TYPE-01 | done | `frontend/src/app/MatchOverlays.tsx`, `frontend/src/screens/deck-browser/DeckBrowserDeckGrid.tsx`, `frontend/src/screens/deck-browser/DeckBrowserFilters.tsx`, `frontend/src/screens/deck-browser/DeckOpenedModal.tsx`, `frontend/src/screens/CardBrowserScreen.tsx`, `frontend/src/screens/DeckBrowserScreen.tsx`, `frontend/src/screens/deck-browser/DeckCardSelectorModal.tsx`, `frontend/eslint.config.mjs`, `frontend/package.json`, `package.json` | `npm run lint` (Linux Node 24.14.0), `npm run test`, `npm run build` (Linux Node 22.12.0) | Replaced the named blanket `any` component boundaries with explicit UI/domain contracts, including modal state setters and overlay callbacks. Hook dependency warnings were resolved rather than suppressed. Lint is clean and strict type/build checks pass. |
| DOC-01 | done | `README.md`, `docs/engine-dependency-map.md` | Current source-tree/import audit, documentation review | Replaced the placeholder README with workspace ownership, install/dev/build/test/lint commands, host-authority and privacy boundaries, deterministic testing guidance, and a concise card-effect workflow. Replaced the stale dependency report with a current ownership/boundary map for the public facade, rule flows, presentation, PvP projection, and tests. |
| EVENT-01 | in_progress | `shared/src/types.ts`, `frontend/src/game/engine/core/events.ts`, `frontend/src/game/engine/flow/{ai/abilityUtils,ai/core,board,combat,energy,evolution,playRules,trainers,turn}.ts`, `frontend/src/game/engine.ts`, `frontend/src/app/animation/{battleEffects,cardFlow}.ts`, `frontend/src/app/animation/{battleEffects,battleEffects.test}.ts`, `frontend/src/app/{gameUiHelpers,gameUiHelpers.test}.ts`, `frontend/src/app/hooks/useLogNotifications.ts`, `frontend/src/pvp/{protocol,stateMirror}.ts`, `backend/src/tests/{aiCombatScenarios,pvpProtocolScenarios}.ts` | 63 AI scenarios, focused battle-effects/card-flow/structured-coin tests (57 frontend tests, including wording-independent lethal/played/hidden-draw/tool/status mapping), PvP protocol test, frontend build/test/lint | Authoritative transitions now emit bounded, ordered, serializable events for attack, ability damage/heal/energy, KO, score, game end, promotion, coin, turn, card movement, energy, evolution, status, and Tool outcomes. Status events now explicitly distinguish `apply` from `clear`, including evolution, retreat, promotion, trainer recovery, end-turn Tool recovery, and paralysis expiry; protocol fixtures verify clear events remain valid and missing actions are rejected. Combat coin results for bonus/draw/discard attacks, multi-coin knockout attacks, and AI coin-flip abilities now join the same transition ledger, and the notification queue consumes those coin events rather than discovering them from English log wording. Player and AI ability resolutions share a transition scope; energy movement abilities and the Riko Kashimoto bench-to-active trainer now emit both source `-1` and active `+1` events. Card play, Tool attachment/discard, trainer draw/search/discard/shuffle/swap, stadium replacement, AI basic/evolution play, attack/ability hand discards, retreat payments and Energy discard costs, deck evolution, active-card shuffles, knockout-to-discard movements, board-normalization overflow discards, end-turn tool healing, poison damage, and bench-to-active energy movement now emit structured events. Regression coverage now also asserts identity-preserving deck-evolution events, public Stadium replacement movements, target-specific status outcomes, and trainer status-clear outcomes. Opponent card identities are projected once a movement reaches public play/discard zones, and battle/card-flow adapters no longer classify visual effects from English log text; state diffs are used only to fill intentionally redacted movement identities/counts. Broader trainer/card-effect event coverage remains pending. |
| EVENT-02 | in_progress | `frontend/src/pvp/stateMirror.ts`, `frontend/src/pvp/protocol.ts`, `frontend/src/app/hooks/usePvpMatch.ts`, `frontend/src/game/engine/flow/turn.ts`, `backend/src/tests/pvpProtocolScenarios.ts` | PvP projection/cursor/history-gap fixture, protocol test, build, lint | Host sync now sends only recipient-visible structured event deltas with a monotonic event cursor and retained-history start marker; guests merge mirrored events by stable ID, clear stale retained presentation events when the marker proves an exhausted-history gap, and retain canonical state as the resync source. Public card movement, attack/damage, Energy, evolution, coin, KO, score, promotion, status, Tool, and game-end transitions remain available to both presentation pipelines; private events and hidden-zone card identities are omitted, while played/discarded opponent card identities remain public. The host-owned PvP turn deadline is now 45 seconds and the UI fallback matches it. Browser reconnect remains unverified. |
| FLOW-01 | in_progress | `frontend/src/app/sequence/visualSequence.ts`, `frontend/src/app/sequence/visualSequence.test.ts`, `frontend/src/app/hooks/useBattleVisuals.ts`, `frontend/src/app/{AppRoot,MatchOverlays}.tsx` | Focused sequence tests (14 tests), frontend type/lint check | A pure precedence reducer now owns the visual phase/blocking decision across coin, battle, KO dissolve, point, promotion, card-flow, and pending-choice states. Battle, card-flow, point-gain, and selection-prompt visibility now derive from that single phase owner instead of repeating independent priority predicates. Battle-batch and point-reward completions now carry their active IDs through the overlay boundary and are rejected when their phase no longer owns presentation or their identity is stale; table tests cover priority, pending-choice handoff, KO-batch precedence, and stale/duplicate completion identities. Existing KO/card-flow state remains in the hook while migration continues. |
| ARCH-03 | in_progress | `backend/src/{config,server}.ts`, `backend/src/start.ts`, `backend/src/routes/{localDeckRoutes,cloudDeckRoutes}.ts`, `backend/src/pvpRoutes.ts`, `backend/src/storage/{localDeckStore,cloudDeckStore}.ts`, `backend/src/tests/{configScenarios,serverConstruction,localDeckStoreScenarios,localDeckRoutesScenarios,cloudDeckStoreScenarios,pvpRoutesScenarios}.ts`, `backend/package.json`, `package.json`, `README.md` | backend build, config/default tests, ephemeral HTTP app tests, isolated local/cloud-fallback/signaling fixtures, injected cloud/Firebase outage/auth rejection responses | `createApp` now owns injectable app construction while `startServer` remains the only startup binding; fixture stores and Firebase health dependencies can be supplied without production credentials. Local and cloud deck persistence now live behind focused, injectable route/store boundaries with malformed-file isolation, input validation, atomic writes, stable JSON 503 responses, preserved cloud API paths, and production-safe no-Firebase behavior. Unauthenticated cloud fallback and backend unlocks now require explicit development opt-in and are forced off in production; local-only API behavior is documented and tested. Cloud route authentication, Firebase token verification, user-profile writes, and the Firestore provider are injectable for boundary tests; invalid tokens return 401, profile persistence outages return 503, and rejected auth is proven not to reach deck persistence. PvP signaling now has injectable clock/TTL coverage for answer/offer compatibility, candidate filtering, and expiration. Broader authentication/persistence-failure coverage remains pending. |
| ARCH-02 | in_progress | `frontend/package.json`, `shared/package.json`, `frontend/src/game/engine.ts`, `frontend/src/game/engine/index.ts`, `frontend/src/app/{AiTelemetryPanel.tsx,animation/{battleEffects,cardFlow}.ts,hooks/useAppRuntimeEffects.ts}`, `frontend/src/match/modals/{CardPreview,DeckChoiceModal}.tsx`, `frontend/src/screens/{CardBrowserScreen.tsx,card-browser/model.ts,deck-browser/{DeckBrowserFilters,DeckCardSelectorModal,helpers,model}.ts}`, `frontend/src/utils/cardSorting.ts`, `frontend/src/pvp/playerIntent.ts`, `backend/src/{server.ts,routes,storage,tests}`, `frontend/src/app/animation/battleEffects.test.ts`, `docs/engine-dependency-map.md` | Deep-import audit, package-resolution scenario, frontend tests, build, lint | The engine facade, shared workspace, and validated PvP/perspective subpaths are exported through workspace package boundaries. Backend routes, storage adapters, rule scenarios, and protocol scenarios now import shared data and frontend engine/PvP boundaries through package exports instead of repository-relative filesystem paths. The package-resolution scenario verifies the shared, engine, and PvP exports, and the dependency map reflects the current facade and flow ownership. Remaining work is auditing any intentional UI-independent engine extraction and avoiding package exports for browser-only code. |
| PERF-02 | in_progress | `frontend/src/app/LazyLoadErrorBoundary.tsx`, `frontend/src/app/LazyLoadErrorBoundary.test.tsx`, `frontend/src/app/nonMatchScreens.tsx`, `frontend/src/app/AppRoot.tsx`, `frontend/src/app/styles.ts`, `frontend/src/components/cards/HoloCardImage.tsx`, `frontend/src/utils/{customisation,assetFallback,assetFallback.test}.ts`, `shared/src/data/cards.json`, `shared/src/gameData.ts`, `frontend/public/assets/{cards,customisation}/**/*.avif`, `frontend/public/fallback/**` | 57 frontend tests, 63 AI scenarios, build, lint; runtime-AVIF-to-fallback filesystem integrity and runtime-folder separation tests plus asset-reference audit | Lazy screen and match-board chunk failures now render a recoverable reload action instead of an unexplained blank/indefinite Suspense state. All 97 card PNGs now have alpha-safe AVIF counterparts, and card references plus generated full-art/gold variants resolve to AVIF while the original PNGs are retained under `frontend/public/fallback/cards`. Playmat and sleeve assets now also use AVIF at runtime; their original PNG/JPG masters are retained under `frontend/public/fallback/customisation`. Card images use AVIF `<picture>` sources with fallback originals, direct sleeve/card images switch to the fallback path on load failure, and playmat backgrounds use an AVIF/legacy image set. Fallback resolution now normalizes absolute browser `currentSrc` URLs, query strings, and encoded filenames before selecting the retained master. Filesystem regressions now verify every runtime AVIF maps to an existing retained fallback file and that runtime `cards`/`customisation` folders contain no legacy PNG/JPG masters. Initial/first-match transfer measurements and asset-cache profiling remain pending. |
| ARCH-01 | in_progress | `frontend/src/app/{AppRoot,MatchOverlays}.tsx`, `frontend/src/app/hooks/{useAppNavigation,useAppRuntimeEffects,useEscapeHotkey,useGameOverPresentation,useMatchCommandController,useMatchModalActions,useMatchZoneModal,useQueuedVisualActions,useMatchTransientReset,useOpeningHandFlow}.ts`, `frontend/src/app/hooks/{useGameOverPresentation,useMatchZoneModal,useQueuedVisualActions,useMatchTransientReset,useOpeningHandFlow}.test.ts` | Frontend tests (57), type/lint check | Deferred visual actions now belong to a focused hook with explicit queue/flush/clear ownership, reducing raw coordination state in `AppRoot`. Setup countdown, opening-coin resolution, and attack-shuffle commands now share a typed command-controller boundary that keeps canonical transition and host-sync behavior together. Opening-coin pending state, opening-hand deal gating, duplicate-animation suppression, and deferred-deal coordination now belong to a focused opening-hand flow hook with regression coverage. Game-over reveal timing and cleanup now belong to a dedicated hook with fake-timer coverage; reset cancels the reveal and clears deferred opening-hand state through that boundary. The complete transient-match reset policy now belongs to a dedicated coordinator with regression coverage; new-match creation and PvP rematches use that single reset rather than repeating individual state mutations. The reset owns AI perspective, POV token, card-flow tracking, queues, retained event state, modal state, and deferred reveal state. Pile and opponent-zone modals now have a dedicated mutually exclusive owner, so Escape, game-over/menu paths, and transient resets cannot leave a revealed opponent-hand or zone modal behind; hook regressions cover replacement and reset behavior. Broader match-controller extraction and browser lifecycle verification remain pending. |
| UX-03 | in_progress | `frontend/src/components/cards/UmaCard.tsx`, `frontend/src/app/{AppRoot,MatchBoardLayout}.tsx`, `frontend/src/app/hooks/{useOpeningHandFlow,useLogNotifications,useMatchDerivedState}.ts`, `frontend/src/match/controls/{SelectionPrompt,SelectionPrompt.test}.tsx`, `frontend/src/app/layout/{matchLayoutBaseline,matchLayoutBaseline.test,matchViewport,matchViewport.test,useElementSize}.ts` | 8 focused layout tests, build, lint, `git diff --check` (Linux Node 22.12.0) | Replaced the active-card HP-corner backdrop-filter/mask combination with the original visual shape minus the compositor-prone backdrop blur, preventing opaque white during card movement. Opening coin presentation now derives from the authoritative structured setup event, while the duplicate text-log fallback cannot block the guest without an animation; setup coins are excluded from the generic coin queue. Local card-effect selections now render their bottom prompt after preceding visuals finish; hand-card and retreat selections retain Cancel, while committed attack/ability targets such as Nishino Flower/Super Creek healing cannot be cancelled but retain mandatory guidance. The Active card now fills its existing padded slot instead of retaining an arbitrary 420px cap, removing the remaining empty frame around it without changing the slot geometry. Scaling stage 1 provides a pure contained-layout calculator, an element-level ResizeObserver hook, and an auditable 1760px-width baseline for the current board shell. The existing match-content shell is measured, but the result and baseline are observational only—no dimensions, styles, animations, or interaction geometry consume them yet. A first board-only visual integration was reverted because it expanded board framing independently of the cards. Direct Chrome/Firefox and two-peer PvP validation remain pending. |
| UX-02 / VERIFY-01 | deferred | — | — | Browser pacing/accessibility acceptance and the final cross-mode/device matrix require direct browser/device evidence. |

## Consolidated remaining work

The following is the ordered path to finish the outstanding improvement work. Items are grouped by dependency so that browser-facing changes remain small and reversible.

1. **Close the current Active-card fit change.** Confirm in Chrome and Firefox at 1920×1080 that the Active card fills its padded frame without clipping the HP overlay, Tool badge, Energy pips, hover state, drag target, or KO animation.
2. **Define responsive-layout acceptance before changing geometry again.** Record the desired visual target at 1366×768, 1920×1080, 2560×1440, 3840×2160, and one ultrawide viewport. The first board-only scaling attempt was reverted because it enlarged framing without enlarging cards. Any renewed implementation must scale the complete visual unit together: card, frame, bench, score controls, Stadium/drop zone, hand, and controls. It must use explicit measured dimensions, retain normal document flow, and verify attack/draw/KO geometry in Chrome and Firefox before moving to the next region.
3. **Complete real two-peer PvP validation.** Exercise host-only opening-coin selection, joiner waiting state, the shared coin animation, public draw/energy/evolution/attack/KO/score visuals, the 45-second deadline, disconnect/reconnect, and hidden-zone privacy. Record actual browser/device outcomes; transport fixtures alone are not sufficient.
4. **Complete browser animation/lifecycle checks.** Run setup, draw, compound trainer, attack, Active/Bench KO, promotion choice, winning KO, reset/menu/rematch during each blocking sequence, and reduced-motion paths. The intermittent pre-KO translucency and any remaining KO hitch must be reproduced before further changes are made.
5. **Finish accessibility and small-screen checks.** Verify keyboard-only play and modal focus return, touch alternatives to hover/drag, long labels, and overflow/hit targets at the recorded small viewport.
6. **Measure before performance work.** Capture browser traces for a busy board, foil hover, multi-card draw, PvP clock, initial menu, and first match. Keep only optimizations supported by those measurements, including the already-completed AVIF migration's actual network/cache behavior.
7. **Finish remaining architecture and rules coverage.** Continue only evidence-driven extractions from the engine/AppRoot, cover remaining structured card-effect events, and retain the public engine/PvP boundaries. Then run the final acceptance matrix from the implementation plan and update each task status with direct evidence.

## Local validation notes

- Latest fast-suite result after the transient-reset, structured-Tool-event, Firebase-injection, pending-choice validation, malformed-choice guard, AVIF fallback-integrity, opening-hand-flow extraction, package-boundary, status-clear event, and canonical-invariant changes: 63 AI scenarios and 57 frontend tests passed; frontend build, lint, and `git diff --check` also pass.
- Cloud auth boundary coverage now includes injected invalid-token (401) and user-profile persistence outage (503) responses, alongside the existing deck-store/Firebase outage checks.
- Static asset audit: the 97 card AVIFs occupy 19.3 MB versus 115.7 MB of fallback PNGs (83.3% smaller); all runtime AVIF assets occupy 20.8 MB versus 121.2 MB of retained PNG/JPG originals (82.8% smaller). This is an on-disk comparison, not a browser transfer/cache measurement.
- The fallback regression also exercises absolute browser `currentSrc` URLs with query strings and encoded filenames, matching the URL shape used by image error events in production.
- Latest responsive-layout/Active-card-fit validation: 8 focused layout tests, frontend production build, lint, and `git diff --check` passed with Linux Node 22.12.0. The visual Active-card fit and all responsive behavior still require direct browser evidence.
- Temporary deployment policy: all currently defined cards are treated as owned by the card browser and deck editor, and new cloud collection seeds grant two copies of every card. Frontend build/lint, backend build, and the cloud-deck scenario pass with this policy. The explicit flags in `frontend/src/config/devUnlocks.ts` and `backend/src/storage/cloudDeckStore.ts` must be removed or replaced when collection progression is introduced.

- The default `npm` on this machine resolves to Windows and fails from this WSL path. Validation used Linux Node 22.12.0 by putting its existing NVM bin directory first in `PATH`; no project configuration was changed for that environment issue.
- The currently active WSL shell resolves `node` to Linux Node 12.22.9, which cannot parse the installed Vitest version. Linux Node 22.12.0 remains available through the existing NVM installation and was used explicitly for the focused metric tests and frontend build.
- The configured lint version requires Linux Node 24.14.0 in this workspace; it completed with no warnings. Build and tests were run with Linux Node 22.12.0.
- This WSL task's desktop automation helper rejects the workspace path before it can enumerate a browser window, so it cannot itself perform direct browser/device checks. Separately, a manual live-browser AI-vs-AI KO check completed cleanly with no visible hitch or stalled transition; the remaining browser-dependent rows stay unverified unless their specific scenario is recorded.
- The linked Windows-native checkout at `C:\\Users\\joshu\\Documents\\umamusume-card-duel` builds its frontend successfully with Windows Node. Its installed dependencies are platform-native, so Linux Node cannot reuse them; direct browser automation still requires the task's desktop-control session to be opened from that Windows-native workspace rather than this WSL-bound task.
- No commits, pushes, pull requests, deployments, or remote-service mutations were performed.
