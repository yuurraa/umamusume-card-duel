/**
 * Compatibility entry point for code that resolves the engine directory.
 * Keep the public surface identical to the sibling `game/engine.ts` facade;
 * rule modules remain private implementation details.
 */
export * from "../engine";
